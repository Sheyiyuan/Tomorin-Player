# 更新日志

## v1.3.0 (2026-03-10)

### 歌词系统
- 全新歌词面板，支持逐行同步高亮与自动滚动。
- 多源歌词搜索（LRCLIB、Bilibili），带置信度评分与自动择优。
- 支持手动导入 LRC/TXT 歌词文件，并提供预览与格式校验。
- 时间轴偏移调节与手动锁定，避免后续自动搜索覆盖。
- 歌词文档版本化管理（LyricDocument + LyricPreference），可靠溯源。

### 收藏库重构
- 收藏歌曲分页加载，支持虚拟滚动，大幅降低大数据量渲染开销。
- Bilibili 收藏夹双向同步：支持从 B 站拉取收藏到本地、将本地收藏回推到 B 站。
- 同步进度可视追踪，任务化异步执行，支持中途取消。
- SongRef 增加 position 字段，收藏内歌曲顺序持久化。
- FavoriteSummary 轻量化列表模型，提升列表页性能。

### 播放器与工作区重设计
- 主面板重构为选项卡布局（队列、歌曲详情、歌词）。
- PlayerBar 重新设计，集成迷你队列弹出框，减少面板切换。
- 当前播放列表卡片支持虚拟化视图，大列表流畅滚动。

### 错误处理
- 新增 AppErrorBoundary 全局错误边界，渲染异常时展示友好的回退 UI。
- 统一 DomainError 错误体系，区分后端领域错误与系统异常，前端精准展示。
- 歌词面板独立 ErrorBoundary，歌词加载异常不影响主界面。

### 主题系统
- 统一主题提供者（ThemedMantineProvider），消除多处重复的颜色方案逻辑。
- 主题编辑器重建：JSON 编辑器替换为原生 Textarea，布局与交互优化。
- ThemedModal 外壳统一应用于所有弹窗，确保主题一致性。
- 颜色方案切换逻辑稳定化，修复浅色/深色模式切换闪烁。

### 后端加固
- 新增 netguard 模块：面向公网的 HTTP 网关，拒绝私有/特殊地址，防止 SSRF。
- 流媒体、歌词、公网请求使用独立 HTTP 客户端，按需限定目标域名。
- Session 状态管理改为结构化 sessionState + sessionTransport，避免全局 cookie 泄漏。
- 数据目录权限收紧为 0700，保护本地凭证与数据库。
- 存储服务新增路径校验，防止目录穿越。
- 代理服务器重构，增强测试覆盖与回退策略。

### 前端架构
- 移除大量遗留 hooks（useAudioV2、usePlayerV2、useAppStore 等），统一数据流。
- 新增工具模块：accessibility（可访问性）、domainError、player、window、bv、audio。
- 窗口控制组件重构，适配跨平台拖拽区域。
- 测试覆盖大幅扩展：新增 100+ 测试文件，覆盖核心 hooks、组件、工具函数。

### 构建与打包
- 质量门禁强化：前端构建作为 Go vet/test 前置条件。
- 构建环境固定为 Node 22 + pnpm 11，消除环境差异。
- macOS 打包迁移至 Go 1.24。
- 采用 Wails 规范应用图标源，修复图标生成与歌曲列表导入问题。

## v1.2.0 (2026-02-05)

### 播放与稳定性
- 共享音频引擎（单例 audio + 引用计数），提升播放稳定性。
- Linux/Wails 播放链路优化，默认禁用 WebAudio 路由以减少无声问题。
- 播放事件处理与播放流程优化。

### 代理与网络
- 音频代理自恢复机制（EnsureAudioProxyRunning）。
- 代理端口改为动态分配，旧版代理 URL 在运行时刷新到当前端口。

### 主题与图片
- 主题背景图本地缓存与图片代理策略优化。

### 工程与文档
- ESLint 配置与前端依赖更新。
- 新增统一 Makefile，覆盖依赖安装、测试、质量检查、开发启动和多平台打包。
- 整合现有平台脚本，Linux DEB/RPM 共用一次原生构建并自动选择 WebKitGTK 版本。
- 更新开发指南与播放说明文档。

## v1.1.0 (2026-01-18)

### 功能
- 多 P 视频获取与标题格式化，支持完整分 P 列表播放。
- B 站搜索体验优化与筛选增强。
- 播放控件标题滚动与 UI 文案对齐优化。
- 主题系统能力扩展（含封面圆角等配置）与编辑体验增强。
- 顶栏支持拖拽（Wails 无边框自定义标题栏）。

### 性能与架构
- 状态管理拆分为多 Context，减少无关重渲染。
- 组件与弹窗系统多阶段性能优化，加入懒加载与条件渲染。
- App 结构重构与聚合 Hook，降低复杂度并提升可维护性。

### 播放与稳定性
- 处理多 P 音频缓存与播放流程。
- 改进音频代理与本地缓存回退策略。
- 自动播放与时长异常（Infinity/NaN）场景修复。
- 修复多处 useEffect/循环依赖引发的状态问题。

### 资源与打包
- Windows 图标生成与适配优化（多尺寸 + 透明背景）。
- macOS ICNS 图标生成支持与脚本兼容性修复。
- CI 图标生成流程修复与构建产物清理。
- deb 包许可证信息完善。

### 文档
- 文档结构整理与性能优化要点补充。
- 新增/修订多份重构与修复总结文档。
